#How to install a library using npm

#1) Open command prompt
#2) Type somthing like:

 npm install --save [LIBRARY_NAME]

#3) Hit enter
#4) The library will likely be saved in a folder called:

 C/:USERS/[CURRENT_USER]/node modules

 #or the cd (current directory) shown in the command prompt.
